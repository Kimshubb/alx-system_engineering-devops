#
# rules.before
#
# Rules that should be run before the ufw command line add
# # Default: deny (incoming), allow (outgoing), disabled (routed)
$ sudo ufw default allow outgoing
$ sudo ufw default deny incoming
$ sudo ufw allow 8080/tcp
$ sudo ufw enable
$ sudo ufw reload
